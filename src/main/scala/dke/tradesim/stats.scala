package dke.tradesim

object stats {
  // returns the sample correlation coefficient (Pearson's r coefficient)
  def correlation(series1: Seq[BigDecimal], series2: Seq[BigDecimal]): BigDecimal = {

  }
}
